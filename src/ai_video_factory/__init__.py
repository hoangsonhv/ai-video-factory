"""AI Video Factory — CLI-first automated video generation pipeline (Idea → MP4).

This package is organized by Clean Architecture layers under a ``src`` layout:
``domain``, ``application``, ``infrastructure``, ``interface`` and ``shared``.
Source dependencies point inward only (see docs/ai-tool.md §3, ADR-006).
"""

__version__ = "0.1.0"
